# Runtime self-heal artifacts, regenerated by the Photon adapter:
# - index.mjs.known-good is snapshotted on every healthy /healthz boot
# - index.mjs.broken-last is saved if a rollback ever fires
# Neither belongs in version control.
index.mjs.known-good
index.mjs.broken-last
